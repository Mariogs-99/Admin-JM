import { LoginForm } from "../components/loginForm"

export const LoginPages = () => {
  return (
    <div className="min-h-screen w-full flex items-center justify-center bg-[#f9f7f5]">
      <LoginForm />
    </div>
  );
};
